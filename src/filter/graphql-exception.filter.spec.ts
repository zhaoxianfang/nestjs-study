import { GraphqlExceptionFilter } from './graphql-exception.filter';

describe('GraphqlExceptionFilter', () => {
  it('should be defined', () => {
    expect(new GraphqlExceptionFilter()).toBeDefined();
  });
});
